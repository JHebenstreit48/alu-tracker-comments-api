{
  "name": "alu-tracker-comments-api",
  "version": "1.0.0",
  "description": "Per-car comments API (guest + logged-in later, pending moderation)",
  "main": "dist/index.js",
  "type": "commonjs",
  "scripts": {
    "start": "ts-node -r tsconfig-paths/register src/server.ts",
    "dev": "ts-node -r tsconfig-paths/register src/server.ts",
    "build": "tsc",
    "start:watch": "nodemon --exec ts-node -r tsconfig-paths/register src/server.ts",
    "start:dev": "npm run start:watch"
  },
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "express-rate-limit": "^8.1.0",
    "helmet": "^8.1.0",
    "mongoose": "^8.18.2",
    "zod": "^4.1.11"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.3",
    "@types/node": "^24.5.2",
    "nodemon": "^3.1.10",
    "ts-node": "^10.9.2",
    "tsconfig-paths": "^4.2.0",
    "typescript": "^5.9.2"
  }
}